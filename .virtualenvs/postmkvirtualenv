#!/usr/bin/env zsh
# This hook is run after a new virtualenv is activated.

#
#easy_install pip

#
pip install virtualenv virtualenvwrapper
pip install pip-tools

#
pip install distribute

# interactive shell
pip install ipython
# pip install bpython

# debug
pip install Werkzeug

# easy_install -U distribute

# debug
pip install pudb debug ipdb
# pip install django-pudebug

# TEST
pip install nose nose-pudb mock tox pytest

# document
# pip install Sphinx

# deployed
pip install South
# pip install Fabric

# peformance, analysis
# pip install pyflakes pep8 reimport pylint
pip install flake8
# pip install clonedigger


#  ******************************************************************************
#  libedit detected - readline will not be well behaved, including but not limited to:
#    * crashes on tab completion
#    * incorrect history navigation
#    * corrupting long-lines
#    * failure to wrap or indent lines properly
#  It is highly recommended that you install readline, which is easy_installable:
#      easy_install readline
#  Note that `pip install readline` generally DOES NOT WORK, because
#  it installs to site-packages, which come *after* lib-dynload in sys.path,
#  where readline is located.  It must be `easy_install readline`, or to a custom
#  location on your PYTHONPATH (even --user comes after lib-dyload).
#  ******************************************************************************

# ipython readline
easy_install readline

echo <<_EOF_

more packages, install to ::

    $ ~/.virtualenvs/django-packages
    $ ~/.virtualenvs/tornado-packages
    $ ~/.virtualenvs/flask-packages

_EOF_

